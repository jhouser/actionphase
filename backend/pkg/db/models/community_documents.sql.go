// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: community_documents.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createCommunityDocument = `-- name: CreateCommunityDocument :one

INSERT INTO community_documents (
    community_id, title, content, status, sort_order, created_by_user_id
) VALUES (
    $1, $2, $3, $4, $5, $6
)
RETURNING id, community_id, title, content, status, sort_order, created_by_user_id, created_at, updated_at
`

type CreateCommunityDocumentParams struct {
	CommunityID     int32       `json:"community_id"`
	Title           string      `json:"title"`
	Content         string      `json:"content"`
	Status          string      `json:"status"`
	SortOrder       int32       `json:"sort_order"`
	CreatedByUserID pgtype.Int4 `json:"created_by_user_id"`
}

// Community documents (req 7, 8).
//
// Two audiences read these: moderators, who see drafts, and everyone else, who
// sees only published documents. That split is expressed as SEPARATE queries
// rather than one query with a boolean flag, so a caller cannot accidentally
// expose drafts by passing the wrong argument -- the handler must choose the
// privileged query explicitly.
func (q *Queries) CreateCommunityDocument(ctx context.Context, arg CreateCommunityDocumentParams) (CommunityDocument, error) {
	row := q.db.QueryRow(ctx, createCommunityDocument,
		arg.CommunityID,
		arg.Title,
		arg.Content,
		arg.Status,
		arg.SortOrder,
		arg.CreatedByUserID,
	)
	var i CommunityDocument
	err := row.Scan(
		&i.ID,
		&i.CommunityID,
		&i.Title,
		&i.Content,
		&i.Status,
		&i.SortOrder,
		&i.CreatedByUserID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteCommunityDocument = `-- name: DeleteCommunityDocument :exec
DELETE FROM community_documents
WHERE id = $1 AND community_id = $2
`

type DeleteCommunityDocumentParams struct {
	ID          int32 `json:"id"`
	CommunityID int32 `json:"community_id"`
}

// Community-scoped, matching the read and update paths.
func (q *Queries) DeleteCommunityDocument(ctx context.Context, arg DeleteCommunityDocumentParams) error {
	_, err := q.db.Exec(ctx, deleteCommunityDocument, arg.ID, arg.CommunityID)
	return err
}

const getCommunityDocument = `-- name: GetCommunityDocument :one
SELECT id, community_id, title, content, status, sort_order, created_by_user_id, created_at, updated_at FROM community_documents
WHERE id = $1 AND community_id = $2
`

type GetCommunityDocumentParams struct {
	ID          int32 `json:"id"`
	CommunityID int32 `json:"community_id"`
}

// Scoped by community as well as id: the id alone would resolve a document
// belonging to a DIFFERENT community than the one in the request path, letting
// a moderator of community A read A's URL into B's draft. The pairing makes
// that a no-rows 404 rather than a permission check the handler has to
// remember.
func (q *Queries) GetCommunityDocument(ctx context.Context, arg GetCommunityDocumentParams) (CommunityDocument, error) {
	row := q.db.QueryRow(ctx, getCommunityDocument, arg.ID, arg.CommunityID)
	var i CommunityDocument
	err := row.Scan(
		&i.ID,
		&i.CommunityID,
		&i.Title,
		&i.Content,
		&i.Status,
		&i.SortOrder,
		&i.CreatedByUserID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listCommunityDocuments = `-- name: ListCommunityDocuments :many
SELECT id, community_id, title, content, status, sort_order, created_by_user_id, created_at, updated_at FROM community_documents
WHERE community_id = $1
ORDER BY sort_order ASC, id ASC
`

// Moderator listing: drafts included.
//
// Ordered by sort_order then id. The id tiebreak is not decoration: sort_order
// defaults to 0, so a community that never sets it would otherwise get an
// arbitrary, unstable order that changes between requests.
func (q *Queries) ListCommunityDocuments(ctx context.Context, communityID int32) ([]CommunityDocument, error) {
	rows, err := q.db.Query(ctx, listCommunityDocuments, communityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CommunityDocument
	for rows.Next() {
		var i CommunityDocument
		if err := rows.Scan(
			&i.ID,
			&i.CommunityID,
			&i.Title,
			&i.Content,
			&i.Status,
			&i.SortOrder,
			&i.CreatedByUserID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPublishedCommunityDocuments = `-- name: ListPublishedCommunityDocuments :many
SELECT id, community_id, title, content, status, sort_order, created_by_user_id, created_at, updated_at FROM community_documents
WHERE community_id = $1 AND status = 'published'
ORDER BY sort_order ASC, id ASC
`

// Public listing: published only. Backs the community page for ordinary
// visitors and the community section of the game Info tab.
func (q *Queries) ListPublishedCommunityDocuments(ctx context.Context, communityID int32) ([]CommunityDocument, error) {
	rows, err := q.db.Query(ctx, listPublishedCommunityDocuments, communityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CommunityDocument
	for rows.Next() {
		var i CommunityDocument
		if err := rows.Scan(
			&i.ID,
			&i.CommunityID,
			&i.Title,
			&i.Content,
			&i.Status,
			&i.SortOrder,
			&i.CreatedByUserID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPublishedCommunityDocumentsForGame = `-- name: ListPublishedCommunityDocumentsForGame :many
SELECT d.id, d.community_id, d.title, d.content, d.status, d.sort_order, d.created_by_user_id, d.created_at, d.updated_at
FROM community_documents d
JOIN games g ON g.community_id = d.community_id
WHERE g.id = $1 AND d.status = 'published'
ORDER BY d.sort_order ASC, d.id ASC
`

// The Info tab's list, resolved from a GAME rather than a community.
//
// Inner join through games.community_id, so a LEGACY GAME (community_id IS
// NULL) yields no rows and the Info tab simply renders no community section.
// That grandfathering lives here rather than in the handler for the same reason
// IsUserBannedFromGameCommunity does: one forgotten NULL check at a call site
// would be a crash or an empty section for a reason nobody could explain.
//
// Returns documents ONLY. The community's name and slug used to ride along on
// every row, back when GetGameWithDetails did not join communities and the tab
// had no other way to name the section. It does join them now, so the game
// payload names its own community and duplicating that identity onto each
// document row would give the Info tab two sources for one fact -- and leave
// the section unable to name a community that has published nothing.
func (q *Queries) ListPublishedCommunityDocumentsForGame(ctx context.Context, id int32) ([]CommunityDocument, error) {
	rows, err := q.db.Query(ctx, listPublishedCommunityDocumentsForGame, id)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CommunityDocument
	for rows.Next() {
		var i CommunityDocument
		if err := rows.Scan(
			&i.ID,
			&i.CommunityID,
			&i.Title,
			&i.Content,
			&i.Status,
			&i.SortOrder,
			&i.CreatedByUserID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateCommunityDocument = `-- name: UpdateCommunityDocument :one
UPDATE community_documents
SET
    title      = COALESCE($1, title),
    content    = COALESCE($2, content),
    status     = COALESCE($3, status),
    sort_order = COALESCE($4, sort_order),
    updated_at = NOW()
WHERE id = $5 AND community_id = $6
RETURNING id, community_id, title, content, status, sort_order, created_by_user_id, created_at, updated_at
`

type UpdateCommunityDocumentParams struct {
	Title       pgtype.Text `json:"title"`
	Content     pgtype.Text `json:"content"`
	Status      pgtype.Text `json:"status"`
	SortOrder   pgtype.Int4 `json:"sort_order"`
	ID          int32       `json:"id"`
	CommunityID int32       `json:"community_id"`
}

// Partial update: each COALESCE leaves its column untouched when the argument
// is NULL, so a PATCH body may carry any subset of fields.
//
// content is NOT nullable in the table, so a bare COALESCE is correct here --
// unlike communities.description, there is no "clear it" state to distinguish
// from "leave it alone". An empty document body is a blank page, not an absent
// one, and it is reachable by sending "".
//
// Scoped by community_id for the same reason GetCommunityDocument is: an id
// from another community must miss rather than write.
func (q *Queries) UpdateCommunityDocument(ctx context.Context, arg UpdateCommunityDocumentParams) (CommunityDocument, error) {
	row := q.db.QueryRow(ctx, updateCommunityDocument,
		arg.Title,
		arg.Content,
		arg.Status,
		arg.SortOrder,
		arg.ID,
		arg.CommunityID,
	)
	var i CommunityDocument
	err := row.Scan(
		&i.ID,
		&i.CommunityID,
		&i.Title,
		&i.Content,
		&i.Status,
		&i.SortOrder,
		&i.CreatedByUserID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
