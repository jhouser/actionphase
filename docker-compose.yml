services:
  # PostgreSQL Database
  db:
    image: postgres:17-alpine
    container_name: actionphase-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
      POSTGRES_DB: actionphase
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/pkg/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d actionphase"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - actionphase-network

  # Go Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: actionphase-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      # Override specific values that need Docker-specific configuration
      DATABASE_URL: "postgres://postgres:example@db:5432/actionphase?sslmode=disable"
      PORT: 3000
      HOST: 0.0.0.0
      RUN_MIGRATIONS: "true"
    volumes:
      - uploads:/root/uploads
      # Archives are NOT in the uploads volume: that one is mounted into the
      # nginx web root, which would serve game archives (containing every
      # private message) to anyone who guessed the path. Downloads must go
      # through the authorizing handler instead.
      - archives:/root/archives
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - actionphase-network

  # React Frontend (nginx)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Pass VITE_ prefixed variables from .env to frontend build
        VITE_HCAPTCHA_SITE_KEY: ${VITE_HCAPTCHA_SITE_KEY:-}
        VITE_HCAPTCHA_ENABLED: ${VITE_HCAPTCHA_ENABLED:-false}
        VITE_FARO_ENABLED: ${VITE_FARO_ENABLED:-false}
        VITE_FARO_URL: ${VITE_FARO_URL:-}
        VITE_FARO_APP_NAME: ${VITE_FARO_APP_NAME:-actionphase}
        VITE_ENVIRONMENT: ${VITE_ENVIRONMENT:-production}
        VITE_COMMENT_MAX_DEPTH: ${VITE_COMMENT_MAX_DEPTH:-5}
        VITE_COMMENT_MAX_DEPTH_MOBILE: ${VITE_COMMENT_MAX_DEPTH_MOBILE:-3}
        GRAFANA_FARO_API_KEY: ${GRAFANA_FARO_API_KEY:-}
        GRAFANA_FARO_SOURCEMAP_ENDPOINT: ${GRAFANA_FARO_SOURCEMAP_ENDPOINT:-}
        GRAFANA_FARO_APP_ID: ${GRAFANA_FARO_APP_ID:-}
        GRAFANA_FARO_STACK_ID: ${GRAFANA_FARO_STACK_ID:-}
    container_name: actionphase-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - uploads:/usr/share/nginx/html/uploads
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - actionphase-network

networks:
  actionphase-network:
    driver: bridge

volumes:
  pgdata:
    driver: local
  uploads:
    driver: local
  # Deliberately not mounted into the frontend container: game archives are
  # private and served only via the authenticated download endpoint.
  archives:
    driver: local
